# Design Directory

Claude adapter note: the shared design rules now live in
`docs/project/design-guidelines.md`.

Use that document as the canonical guidance for any runtime. In Claude Code,
you can still use `/design-review`, `/review-all-gdds`, `/quick-design`,
`/ux-design`, and `/ux-review` as adapter-specific workflow entrypoints.
