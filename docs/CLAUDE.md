# Docs Directory

Claude adapter note: the shared docs rules now live in
`docs/project/docs-guidelines.md`.

Use the neutral ADR template at `docs/project/templates/architecture-decision-record.md`.

In Claude Code, `/architecture-decision` and `/architecture-review` remain the
current adapter-specific workflow helpers.
