# Source Directory

Claude adapter note: the shared source-code rules now live in
`docs/project/source-guidelines.md`.

Use that document as the canonical guidance for any runtime. In Claude Code,
`/test-setup` remains the adapter-specific helper for scaffolding the test layer.
